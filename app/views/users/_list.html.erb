<div>
      <h1>User info #_list</h1>
      <%= image_tag @user.get_profile_image(100,100) %>
      画像が出ない
      <table class ="table table-striped">
        <tr>
          <th>name</th>
          <th><%= @user.name %></th>
        </tr>
        <tr>
          <th>introduction</th>
          <th> <%= @user.introduction%></th>
        </tr>
      </table>
      <% if @user.id ==  current_user.id %>
        <p><%= link_to "Edit", edit_user_path(@user), class: "btn btn-secondary"  %></p>
       <% end %>
</div>

