<div>
  <%= flash[:notice] %>
</div>

<h1>仮User info#edit</h1>

<% if @user.errors.any? %>
   <% if @user.errors.count == 1 %>
    <%= @user.errors.count %> error prohibited this obj from being saved:
  <% else %>
    <%= @user.errors.count %> errors prohibited this obj from being saved:
  <% end %>
  <ul>
    <% @user.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

<%= form_with model: @user do |f| %>
  <label for="inputName">Name</label>
  <p><%= f.text_field :name, autofocus: true, id:"inputName", placeholder:""%></p>

  <p><label for="inputImage">Image</label></p>
  <p><%= f.file_field :profile_image, placeholder: "プロフィール画像", accept: "image/*" %></p>

  <label for="inputName">仮Introduction</label>
  <p><%= f.text_field :introduction, autofocus: true, id:"inputName", placeholder:""%></p>
  


  <p><%= f.submit "Update User" %></p>

  
<% end %>